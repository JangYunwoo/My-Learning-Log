python -m venv 가상환경명
source venve/Scripts/activate

- 장고가 설치되어 있지 않은 경우 > 프로젝트를 처음 시작하는 경우
 pip install django
 pip freeze >> requirements.txt
 django-admin startproject 프로제그명 .

- 필요한 패키지 목록이 이미 있는 경우 > requirements.txt
 pip install -r requirements.txt

- Django app 추가
python manage.py startapp 앱이름
이후 settings.py에서 앱 등록 필요

개발 순서
1. 기능을 수행할 url 등록
ㄱ. main app의 urls.py > 분배(include)
ㄴ. 기능을 수행할 app의 urls.py
    > app_nama, path의 name 달아주기

2. views에 함수 등록
   > 기능을 수행할 app에서만 등록

3. templates 완성 후 return (render)
  Data 묶음만 전달(REST API)

- DTL
1. templates > 전역 공간에 넣고 싶다면 settings.py - DIRS 추가
2. 상속 > extends +block
3. 변수 > {{ }}

- html Form
 > input + submit
 > input > name
 > form 태그의 actions 속성으로 명령 요청
 > form method > GET / POST

 > request.GTE(/POST) > queryDict